# Extend Everything

This is a sample repository that sets up an Umbraco ~~15~~ 16 back office extension
that tries to do a bit of everything.

A greate sample password and username would be `jack.sparrow@xlent.se` and `jack.sparrow123`.
