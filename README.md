# Extend Everything

This is a sample repository that sets up an Umbraco ~~15~~ ~~15~~ 17 back office extension
that tries to do a bit of everything.

A greate sample password and username would be `jack.sparrow@pirates.com` and `password123`.
